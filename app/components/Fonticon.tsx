import React, { Component } from 'react';

const Fonticon = (props) => {
    const {
      className
    } = props;

    return (
        <i className ={className} ></i>
    );

}

export default Fonticon;